<ul id="directions" class="nxs-items">
	<% @directions.each do |d| %>
	<li class="nxs-section"><a href="/<%=params[:route_type] + "/" + params[:route_id] + "/" + d.direction_id.to_s %>"><%="To: " + d.direction_name %></a></li>
	<% end %>
</ul>